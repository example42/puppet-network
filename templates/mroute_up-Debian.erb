#!/bin/bash
#
###
### File managed by Puppet
###
if [ "$IFACE" = "<%= @interface -%>" ] || [ "$IFACE" = "--all" ]; then
<% @routes.each do |net,gw| -%>
  ip route show | grep -q "<%= net %>" || ip route add <%= net %> <% if /^\d/.match(gw) %>via<% else %>dev<% end %> <%= gw %>
<% end -%>
fi
