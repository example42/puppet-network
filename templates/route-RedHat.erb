###
### File managed by Puppet
###
if [ "$IFACE" = "<%= @interface -%>" ]; then
<%- (0..(@ipaddress.length-1)).each do |id| -%>
    <%= @ipaddress[id] %>/<%= @netmask[id] %> via <%= @gateway[id] %> dev <%= @interface %><%- if @table[id] -%>table <%= @table[id] %><%- end -%>
<%- end -%>
fi
